if SHELL

config ZMK_KEYMAP_SHELL
bool "Keymap shell commands"
default y

config ZMK_KEYMAP_SHELL_SLOTS
int "Maximum stored keymaps"
default 4

config ZMK_KEYMAP_SHELL_SLOT_NAME_MAX
int "Maximum slot name length (including terminator)"
default 24

endif
